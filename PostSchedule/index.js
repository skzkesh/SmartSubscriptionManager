// ga dipake

// import { AppRegistry } from 'react-native';
// import App from './App';  // Path to your main App component (ensure this is correct)
// import { name as appName } from './app.json';  // This ensures the correct app name from app.json

// AppRegistry.registerComponent(appName, () => App);  // Registers the App component
